
export default function ProfileLayout({ children })
{
    return (
        <div className="h-full items-center justify-center bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-purple-400 to-purple-900">{children}</div>
    )
}

  