

export default function HomeLayout({ children })
{
    return (
        <div className="h-full items-center justify-center bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-blue-400 to-blue-500">{children}</div>
    )
}