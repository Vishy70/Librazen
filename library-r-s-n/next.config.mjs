/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        remotePatterns: [
            {
                protocol: "https",
                hostname: "laeqipmuqwavuzpdesoe.supabase.co"
            }
        ]
    }
};

export default nextConfig;
